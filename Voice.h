#ifndef VOICE_H
#define VOICE_H

#include <curl/curl.h>
#include <string>

class Voice {
private:

public:
    void Voice::convertBlob(std::string blobPath);
    void Voice::sendToReplicate(std::string blob);
};




#endif